# Used by `npm run build` (which runs `vite build --mode prd`, a custom mode - NOT the Vite
# default "production", so a file named .env.production would never be loaded by this script).
# Left blank: there is no deployed backend to point at yet. Set this before building for a real
# deployment.
VITE_API_BASE_URL=
