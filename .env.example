# Base URL of the Level5Backend API, no trailing slash.
# Local dev: see .env.development (already points at the local backend).
# Production build (`npm run build` uses --mode prd, see .env.prd): set this to the real
# deployed backend URL once one exists.
VITE_API_BASE_URL=http://localhost:5124
