# Committed but auto-generated by openapi-typescript (issue #4) - never hand-edited, and never
# reformatted either, so it stays byte-for-byte what `npm run contracts:generate` produces and
# `npm run contracts:check` (drift check) can trust a plain diff.
src/generated/
