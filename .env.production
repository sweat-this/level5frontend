# Used by `npm run build` (`next build` natively loads .env.production).
# Left blank: there is no deployed backend to point at yet. Set this before building for a real
# deployment.
NEXT_PUBLIC_LEGACY_API_BASE_URL=
