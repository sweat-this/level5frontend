# Used by `npm run dev` (default Vite mode is "development").
# Level5Backend's CORS policy allows any loopback origin/port, so this works regardless of
# which port Vite picks.
VITE_API_BASE_URL=http://localhost:5124
